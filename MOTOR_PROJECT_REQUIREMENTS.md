# 双电机控制工程完整需求说明

## 项目总体目标

在 CCS Theia 中完成并持续调试一个基于“天猛星 MSPM0G3507 + TB6612FNG + 两个直流减速电机 + 两组 AB 编码器”的双电机闭环控制工程，并使用 Git 管理版本。

工程使用 `mspm0-ccs` skill 的规范：

- 外设、引脚和中断通过 SysConfig 配置。
- 不手动修改 SysConfig 生成文件。
- 使用 DriverLib 操作 GPIO、PWM、定时器、UART 和 QEI。
- 每次修改后进行 SysConfig 验证和 CCS 编译。
- 硬件烧录、实际电机表现与源代码编译验证分开说明。

## 硬件连接与编码器

电机驱动：

- 左轮使用 TB6612 A 通道。
- 右轮使用 TB6612 B 通道。
- PWM 频率为 20 kHz。
- PWM 定时器满量程为 1600。

编码器最终状态：

- 左编码器：PB0/PB1，GPIOB 双边沿中断，软件正交解码。
- 右编码器：PA29/PA30，TIMG8 硬件 QEI。
- 左编码器中断重新启用，目的是与右侧硬件 QEI 对比，判断高速计数异常是不是 GPIO 中断漏计造成的。
- 左轮慢速正常、高速少计时，应重点怀疑 GPIO 中断频率过高或 ISR 期间丢失边沿。
- 右轮使用硬件 QEI，主要解决一圈约 60000 个计数时高速 GPIO 中断处理不过来的问题。
- 右侧 16 位 QEI 计数器通过模 65536 差分扩展为 64 位累计总数。
- 单个 10 ms 周期内的绝对变化必须小于 32768，否则无法从 16 位计数差判断真实方向。

## 控制量的最终定义

整个 PID 不再使用 RPM，也不需要实际速度换算。

控制周期为：

```text
10 ms，100 Hz
```

控制量定义为：

```text
目标值 = 每10ms期望的编码器变化值
反馈值 = 当前10ms实际编码器变化值
误差   = 目标值 - 反馈值
```

例如：

```text
RSPD,300
```

表示右轮每 10 ms 期望产生 300 个编码器计数，不是 300 RPM，也不是 PWM 百分比。

目标值要求：

- 命令收到后立即应用。
- 不允许目标斜坡。
- 设置多少就显示多少。
- 当前目标绝对值限制为 3000。

## PID 算法

PID 最终要求使用：

```text
error = targetDelta - encoderDelta

integralError += error × 0.01
integralOutput = Ki × integralError

derivative = -(measurement - previousMeasurement) / 0.01

output = Kp × error
       + integralOutput
       + Kd × derivativeFiltered
```

其中乘以 `0.01` 是因为控制周期为 10 ms，即 0.01 秒。

如果使用不乘时间的离散积分形式，则 Ki 必须包含采样周期。例如当前连续形式的 `Ki=0.3`，等效的每周期离散 Ki 是 `0.003`。

其他要求：

- I 项保存的是未乘 Ki 的累计误差。
- 串口输出的是实际 I 项贡献，即 `Ki × integralError`。
- 输出饱和时启用条件积分抗饱和。
- 修改单独的 Kp、Ki、Kd 时，不清空另外两个参数。
- 运行中修改参数不再清空积分和微分历史。
- 目标为零、STOP、故障时才清零 PID 状态。

## PWM 定义

PWM 不再使用百分比，而是直接使用定时器计数。

```text
硬件PWM满量程：1600
当前软件限制：-1280～+1280
```

当前保留原来的功率安全限制，因此最大实际 PWM 为：

```text
+1280 或 -1280
```

串口显示的是：

- PID 计算后经过限幅；
- 四舍五入；
- 真正写入 PWM 比较逻辑的有符号数值。

不是百分比，也不是限幅前的浮点结果。

## 串口命令

保留原命令名称，避免 VOFA 控件失效：

```text
LSPD,<左目标增量>
RSPD,<右目标增量>

LPID,<kp>,<ki>,<kd>
RPID,<kp>,<ki>,<kd>

LKP,<kp>
LKI,<ki>
LKD,<kd>

RKP,<kp>
RKI,<ki>
RKD,<kd>

STOP
```

虽然命令仍叫 `LSPD/RSPD`，但参数已经不是 RPM。

`STOP` 要求立即：

- 目标归零；
- PID 状态清零；
- PWM 归零；
- 方向脚归零；
- STBY 拉低；
- 停止状态锁存。

## VOFA+ 最终通道定义

串口使用 FireWater 文本协议，115200 8N1，50 Hz 输出，共16列：

| 通道 | 内容 |
|---|---|
| CH0 | 左轮当前目标编码器变化值 |
| CH1 | 左轮参与 PID 的10 ms编码器变化值 |
| CH2 | 左轮实际有符号 PWM 计数 |
| CH3 | 左编码器累计总数 |
| CH4 | 左轮 Kp |
| CH5 | 左轮 Ki |
| CH6 | 左轮 Kd |
| CH7 | 左轮 I 项实际输出 |
| CH8 | 右轮当前目标编码器变化值 |
| CH9 | 右轮参与 PID 的10 ms编码器变化值 |
| CH10 | 右轮实际有符号 PWM 计数 |
| CH11 | 右编码器累计总数 |
| CH12 | 右轮 Kp |
| CH13 | 右轮 Ki |
| CH14 | 右轮 Kd |
| CH15 | 右轮 I 项实际输出 |

## 当前调试重点

目前最需要验证的是编码器高速计数：

1. 先手动慢速转动左轮，检查 CH1 和 CH3。
2. 再逐渐提高左轮速度。
3. 同时观察右轮硬件 QEI 的 CH9 和 CH11。
4. 如果左轮慢速准确、高速少计，而右轮正常，基本可以确认 GPIO 中断路径是瓶颈。
5. 如果左右都异常，则继续检查编码器电平、接线、信号质量、10 ms 取样差分和计数方向。

本文档记录了本会话中最终确认的完整工程行为、接口定义和调试目标。
